export default function CalendarPage() {
  return (
    <div className="space-y-3">
      <h2 className="text-xl font-semibold text-white">Calendar</h2>
      <p className="text-sm text-zinc-400">
        Graph-backed calendar sync lands in Phase 2. Events are stored in the <code className="text-zinc-300">Event</code> model with
        categories for medical, birthdays, and kids clubs.
      </p>
    </div>
  );
}
